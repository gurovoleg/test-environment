module.exports = {
  singleQuote: true,
  printWidth: 80,
  tabWidth: 2,
  semi: true,
  quoteProps: "consistent", // if at least one property in an object requires quotes, quote all properties
  bracketSpacing: true, // print spaces between brackets in object literals
  bracketSameLine: false, // put the > of a multi-line HTML element at the end of the last line instead of being alone on the next line
  arrowParens: "always", // include parentheses around a sole arrow function parameter  - (x) => x
  singleAttributePerLine: false, // enforce single attribute per line in HTML and JSX.
};
